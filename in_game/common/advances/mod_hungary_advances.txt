# moved from age 5 to 4
# halved effects to balance
REPLACE:hun_curtailed_peasantry = {
	potential = { has_or_had_tag = HUN }
	
	age = age_4_reformation
	peasants_estate_max_tax = 0.05
	global_max_rgo_size_modifier = 0.1
	requires = rgo_size_advance_reformation
}